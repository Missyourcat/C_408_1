C 语言提供了 13 种类型的运算符，如下所示。
（1）算术运算符（+ - * / %）。
（2）关系运算符（> < == >= <= !=）。
（3）逻辑运算符（! && ||）。
（4）位运算符（<< >> ~ | ^ &）。
（5）赋值运算符（=及其扩展赋值运算符）。
（6）条件运算符（?:）。
（7）逗号运算符（,）。
（8）指针运算符（*和&）。---讲指针时讲解
（9）求字节数运算符（sizeof）。
（10）强制类型转换运算符（(类型)）。
（11）分量运算符（. ->）。---讲结构体时讲解
（12）下标运算符（[]）。----讲数组时讲解
（13）其他（如函数调用运算符()）。---讲函数时讲解
